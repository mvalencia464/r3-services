---
// src/components/CTABanner.astro
import { SITE } from '../config/site';
---

<section class="cta-banner">
  <div class="container cta-banner__inner">
    <div class="cta-banner__text">
      <h2>Need Commercial HVAC or Equipment Service Fast?</h2>
      <p>R3 Services is ready for urgent diagnostics, refrigeration issues, and critical kitchen equipment repairs.</p>
    </div>
    <a href={`tel:${SITE.phone.replace(/\D/g,'')}`} class="btn btn--primary cta-banner__btn">
      Call {SITE.phone}
    </a>
  </div>
</section>

<style>
  .cta-banner {
    background: linear-gradient(120deg, rgba(22, 22, 27, 0.94), rgba(16, 16, 20, 0.94));
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 3rem 0;
  }

  .cta-banner__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .cta-banner__text h2 {
    color: var(--text);
    font-size: clamp(1.5rem, 3vw, 2.2rem);
  }

  .cta-banner__text p {
    color: var(--muted);
    margin-top: 0.3rem;
    font-size: 1.05rem;
  }

  .cta-banner__btn {
    font-size: 1.15rem;
    padding: 1rem 2.5rem;
    white-space: nowrap;
    flex-shrink: 0;
  }
</style>
